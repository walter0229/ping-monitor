# EXANET Ping Monitor PC 이관 가이드

이 가이드는 현재의 프로그램을 다른 Windows PC(남는 PC 또는 서버)로 옮겨서 실행하는 방법을 설명합니다.

## 1. 파일 준비
현재 작업 중인 `d:\AI\Ping` 폴더의 전체 내용을 압축하거나 복사하여 대상 PC의 원하는 위치로 옮깁니다.
*   `.venv` 폴더는 옮기지 않아도 됩니다 (대상 PC에서 새로 생성하는 것이 더 안전합니다).
*   그 외 `app.py`, `index.html`, `requirements.txt`, `*.bat` 파일들은 모두 포함되어야 합니다.

## 2. 대상 PC 설정 (신규 PC)
1.  **Python 설치 확인**: 대상 PC에 Python(3.9 이상 권장)이 설치되어 있어야 합니다.
2.  **환경 구성**: 폴더 내의 `install_windows.bat` 파일을 더블 클릭하여 실행합니다.
    *   가상 환경(`.venv`)이 자동으로 생성되고 필요한 라이브러리가 설치됩니다.
    *   완료될 때까지 기다려 주세요.

## 3. 프로그램 실행
1.  환경 구성이 완료되면 `run_windows.bat` 파일을 더블 클릭합니다.
2.  검은색 콘솔 창이 뜨면서 서버가 시작되고, 잠시 후 브라우저(`http://localhost:8000`)가 자동으로 열립니다.

## 4. 모바일 및 다른 PC에서 접속하기 (외부 접속)
이 프로그램이 설치된 PC와 같은 와이파이(공유기)를 사용하는 기기라면 어디서든 접속 가능합니다.

1.  **서버 PC의 IP 확인**: 
    - 키보드의 `Win + R`을 누르고 `cmd`를 입력해 실행합니다.
    - `ipconfig` 명령어를 입력하고 `IPv4 주소` 항목을 찾습니다. (예: `192.168.0.15`)
2.  **접속 주소**: 
    - 스마트폰이나 노트북의 브라우저 주소창에 `http://[확인한IP]:8000`을 입력합니다.
    - 예: `http://192.168.0.15:8000`

## 6. 외부(인터넷)에서 접속하고 싶을 때
기본적으로 위 방법은 **같은 와이파이(공유기)** 내에서만 작동합니다. 만약 외부에서 접속하고 싶다면 다음 중 하나가 필요합니다.

1.  **공유기 포트 포워딩**: 공유기 설정에서 8000번 포트를 서버 PC로 연결합니다. (가장 표준적이지만 설정이 복잡할 수 있음)
2.  **Tailscale (추천)**: 서버 PC와 스마트폰에 Tailscale 앱을 깔면, 어디서든 서버 IP로 보안 접속이 가능합니다. (가장 쉽고 안전함)
3.  **ngrok**: 임시로 외부 주소를 생성해 주는 도구입니다.

---
문제 발생 시 현재 모니터링 중인 PC의 Python 버전과 경로 설정을 확인해 주세요.
